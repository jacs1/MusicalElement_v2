<%# binding.pry %>
<!-- <div class="wrap"> -->
    
    <div class="content container-fluid" style="position: relative; z-index: 3">
        <div class="row-fluid">
            <div class="span2">
                <!-- <section class="widget large"> -->
                    <!-- <div class="body"> -->
                        <!-- <div id="sources-chart-pie" class="chart pie-chart"> -->
                            <!-- <ul class="thumbnails"> -->
                              <!-- <li class="span4"> -->
                              <a href='<%= artist_path(artist.id) %>'>
                                <div class="thumbnail">
                                  <img src="<%= album.image %>" alt="">
                                 </div>
                               </a>
                              <!-- </li> -->
                            <!-- </ul> -->
                        <!-- </div> -->
                    <!-- </div> -->
                <!-- </section> -->
            </div>
            <!-- <li class="divider"></li> -->
            
            <div class="span9">
                <section class="widget">
                    <header>
                      <header>
                          <h2 class="page-title"><%= album.name %></h2>
                      </header>
                    </header>
                    <div class="body">
                        <table class="table table-hover">
                            <thead>
                            <tr>
                              <th>#</th>
                              <th>Title</th>
                              <th>Featured Artist</th>
                              <th>Length</th>
                              <th>Size</th>
                              <th></th>
                              <th>Rating</th>
                            </tr>
                            </thead>
                            <% album.tracks.each do |t| %>
                            <tbody>
                            <tr>
                              <td><%= t.track_number.to_s.rjust(2, '0') %></td>
                              <td><%= t.title %></td>
                              <td><%= t.artists.map{ |a| [a][0].name }.join(", ") %></td>
                              <td><%= "#{((t.length) / 60)}:""#{(t.length % 60)}" %></td>
                              <td><%= "#{t.size} MB" %></td>
                              <td>
                                <div class="btn-group btn-group-hover pull-right">
                                  <a class="btn btn-mini" href="#"><i class="icon-play"></i></a>
                                  <a class="btn btn-mini" href="#"><i class="icon-volume-up"></i></a>
                                  <a class="btn btn-mini" href="#"><i class="icon-trash"></i></a>
                                </div>
                              </td>
                            </tr>
                            </tbody>
                            <% end %>
                        </table>
                    </div>
                </section>
            </div>
        </div>
    </div>
<!-- </div> -->

