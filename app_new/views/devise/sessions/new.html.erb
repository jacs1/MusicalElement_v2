<!DOCTYPE html>
<html>
<head>
    <title>Sign In - MusicalElement</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">
    <meta charset="utf-8">
</head>
    <body>
        <div class="single-widget-container">
            <section class="widget login-widget">
                <header class="text-align-center">
                    <h4>Login to your account</h4>
                    <%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
                </header>
                <div class="body">
                    <form class="no-margin"
                          action="index.html" method="get">
                        <fieldset>
                            <div class="control-group no-margin">
                                <label class="control-label" for="email" >Email</label>
                                <div class="control">
                                    <div class="input-prepend input-padding-increased">
                                        <span class="add-on">
                                            <i class="eicon-user icon-large"></i>
                                        </span>
                                        <%= f.email_field :email, :id => 'email', :placeholder => "Your Email", :autofocus => true %>
                                    </div>
                                </div>
                            </div>

                            <div class="control-group">
                                <label class="control-label" for="password" >Password</label>
                                <div class="control">
                                    <div class="input-prepend input-padding-increased">
                                        <span class="add-on">
                                            <i class="icon-lock icon-large"></i>
                                        </span>
                                        <%= f.password_field :password, :id => 'password', :placeholder => "Your Password" %>
                                    </div>
                                </div>
                            </div>
                        </fieldset>
                        <div class="form-actions">
                          <%= button_tag( :class => "btn btn-block btn-large btn-danger") do %>
                            Sign In <span class="small-circle"><i class="icon-caret-right"></i></span>
                          <% end %>
                            <% if devise_mapping.rememberable? -%>
                            
                            <label class="checkbox"><%= f.check_box :remember_me, :class => "checkbox" %>Remember Me?</label>

                            <% end -%>
                            <div class="forgot"><a class="forgot" href="<%= new_password_path(resource_name) %>">Forgot Username or Password?</a></div>
                        </div>
                    </form>
                </div>
                <footer>
                    <div class="facebook-login">
                        <a href="<%= root_path %>"><span><i class="icon-facebook-sign icon-large"></i> LogIn with Facebook</span></a>
                    </div>
                    <% end %>
                    <%= render "devise/shared/links" %>
                </footer>
            </section>
        </div>
    </body>
</html>